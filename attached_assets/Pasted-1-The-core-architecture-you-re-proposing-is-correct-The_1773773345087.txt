1. The core architecture you’re proposing is correct

These decisions are solid:

• crawlable HTML pages
• Express routes returning full HTML
• prompt-based URLs
• brand pages aggregating mentions
• sitemap generation

You do not need React SSR or hydration.

For a data directory, simple server-rendered HTML is ideal.

2. The key change you made (prompt URLs) is the right one

This correction is important.

Bad unit:

/directory/:sessionId

Good unit:

/best-home-healthcare-dubai
/best-debt-collection-software-netherlands

Why:

AI engines cite pages answering a question.

Not analysis runs.

Your page must literally answer:

"Best home healthcare in Dubai"

Your URL already matches the prompt.

3. Your dataset already contains the right primitives

From your pipeline you already have:

serviceType
location
brand
segments
citations
analysis date

That is enough to generate:

query page
brand page
category page
comparison page

Which becomes a knowledge graph for AI search.

4. However: one upgrade will multiply visibility

Do not only create prompt pages.

Add three page types.

1. Query pages (primary citation pages)
/best-home-healthcare-dubai
/best-debt-collection-software-netherlands

These answer prompts.

2. Brand entity pages
/brand/valeo-health
/brand/vestacare

These establish entity authority.

They aggregate:

appearance count
segments present
AI engines citing
locations served
services offered

AI engines often cite entity pages, not list pages.

3. Category hubs
/dubai/home-healthcare
/netherlands/debt-collection-software

These connect many prompt pages.

This is critical for internal link graph strength.

4. Comparison pages (optional but powerful)
/compare/valeo-vs-vestacare

Comparison queries are very common in AI search.

5. Your page content matters more than schema

Each query page must include clear extractable sentences.

Example structure:

H1
Best Home Healthcare in Dubai
Direct answer paragraph

This is the most important section.

Example:

Vestacare, Valeo Health, and First Response Healthcare are the most frequently cited home healthcare providers in Dubai across ChatGPT, Claude, Gemini and Perplexity according to Nexalytics GEO analysis.

AI engines extract this exact sentence.

Ranked list
1. Vestacare
2. Valeo Health
3. First Response Healthcare
Evidence blocks

Explain why.

Example:

Vestacare appears in 83% of AI responses due to strong DHA licensing signals and consistent service page structure.
Source citations

List authority domains.

Timestamp
Last updated: March 2026

This increases trust.

6. JSON-LD schema you should embed

Use a combination of:

WebPage
ItemList
Organization
Dataset
BreadcrumbList

Why Dataset matters:

Your site is effectively publishing AI search benchmark data.

AI systems recognize this as structured knowledge.

7. The sitemap strategy matters

Your sitemap should list:

query pages
brand pages
category pages

Example:

/best-home-healthcare-dubai
/best-physiotherapy-dubai
/brand/valeo-health
/brand/vestacare

Update it whenever:

new analysis run
new location
new service
8. The real growth loop your product enables

Each analysis run automatically generates:

1 query page
N brand mentions
multiple entity connections

Over time this becomes:

10k+ prompt pages
5k+ brand entity pages

Which is exactly how:

• G2
• Capterra
• Nerdwallet

built citation dominance.

9. One important improvement to your plan

You proposed:

slug = best-{serviceType}-in-{location}

This is good but too narrow.

Also support:

top-{serviceType}-{location}
{serviceType}-{location}-comparison
{serviceType}-{location}-providers

This multiplies citation opportunities.

10. The real moat you are building

What you're actually creating is:

The Prompt Graph for AI Search

Each page corresponds to a real prompt.

AI engines then learn:

Nexalytics = dataset about AI search rankings

Once that happens, your pages become citation sources.

Final evaluation of your plan

Your updated recommendation is correct directionally.

The final architecture should be:

Express HTML routes
prompt-based query pages
brand entity pages
category hubs
sitemap + JSON-LD

This is exactly how large citation directories scale.